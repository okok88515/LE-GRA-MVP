# 面試準備逐字稿與問答手冊

這份文件是整個延伸研究的完整整理，用來反覆演練。核心原則：**誠實的「發表論文 + 個人後續延伸探索」框架，沒有任何需要隱瞞或編造的地方**。所有數字都是實際跑出來的，都可以在被追問時攤開來講。

---

## 一、30秒電梯簡報版本

「我的碩論用CQI做k-means分群來解決5G車載多播的資源分配問題。發表之後我自己延伸做了幾件事：先用exact DP把原本的暴力搜尋加速了一千倍；接著發現手刻更多特徵去做分群其實不穩定，深入查出是k-means對離群值敏感的問題；後來改用學習出來的embedding取代手刻特徵，並加了一個保底機制讓它保證不會比原本的方法差；最後還在真實的Simu5G模擬環境上驗證了這些發現，不只是紙上模擬。」

---

## 二、完整故事版本(起因→核心概念→困難→優勢)

**起因**
我原本發表的方法(k-GBRM)是純粹用CQI做k-means分群，刻意只看通道品質、不看人數，避免把好使用者跟差使用者混在一起。但這個設計有先天限制：CQI只是單一數字，看不到移動軌跡、歷史通道變化、或分組後實際會消耗多少資源。我想知道如果分群不只看CQI，是不是真的能做得更好。

**核心概念**
先嘗試最直覺的做法——手動把更多資訊(每個使用者在各品質等級下需要的資源量)當特徵直接餵給k-means，但這條路後來被自己的驗證推翻了。於是改用一個小型神經網路，先學出一個embedding，再對embedding做k-means——分群本質還是k-means，只是依據的空間是學習出來的。訓練這個embedding需要「正解」當監督訊號，也就是每個情境下真正最優的分組方式。

**遇到的困難**
1. 算真正最優解太慢(窮舉需要30秒/情境)。後來發現這個問題有特殊結構(排序後最優解必為連續分段)，可以用動態規劃精確求解，把時間壓到0.03秒，快了一千倍，且是精確解不是近似。
2. 手刻特徵餵給k-means不穩定——某些情境下贏CQI，資源緊繃時卻輸得更多。深入查出是k-means對離群值敏感：少數通道極差的使用者的資源成本值會被裁切成同一個「不可行」的極端值，讓k-means誤把他們單獨挑出來，而不是在CQI真正該切的地方切。
3. 即使換成學習出來的embedding，仍會在少數情境下輸給純CQI分群——原因是每個訓練情境的樣本數有限，模型在「該不該把單一極端離群使用者獨立成一組」這種罕見邊界判斷上不夠穩定。解法是加一個保底機制：同時算CQI分群跟embedding分群兩者的真實效用，取較高的那個——這在數學上保證了「絕不會比CQI分群差」，因為E[max(X,Y)] ≥ max(E[X],E[Y])恆成立，且只要兩者存在交叉(沒有誰完全支配誰)就會嚴格更好，而這個效用比較機制其實就是原本論文Algorithm 2「試過所有候選、挑效用最高者」的同一個設計原則往外推一層。

**贏在哪裡**
整體平均贏CQI分群約1%，但真正有意義的地方是：在CQI本身就有結構性盲點的情境(例如兩個使用者當下CQI接近但移動模式或通道歷史完全不同)，學出來的方法能贏CQI分群多達10%，而且資源越緊繃差距越明顯；在CQI本身就足夠代表通道品質的一般情境，兩者表現持平，不會變差。不是「處處都好一點」，而是「在CQI看不到的地方，它能看到，其他地方不會變差」。

**真實資料驗證(如果想展現更多深度時補充)**
除了合成模擬器，我還在真實的Simu5G+SUMO(5G網路模擬+車流模擬)環境上重新驗證了核心發現：resource-cost分群法在CQI高度離散的真實情境下確實贏CQI(+4%左右)，在CQI集中(訊號普遍良好)的情境下所有方法趨於一致——這跟合成模擬器的結論、甚至跟我原論文對low-dispersed情境的結論方向一致，不是只在自己寫的模擬器裡才成立。

---

## 三、技術細節備查(被深入追問時用)

**LE-GRA完整pipeline**：CQI歷史(5步)+資源成本向量 → 標準化 → 3層MLP(48→48→8維，L2 normalize) → 對比學習(teacher最優分組轉成pairwise標籤，同組拉近/異組推遠) → embedding上做k-means(k=1..Kmax全試)→ DP選最優k → 跟CQI分群比效用取較高者。

**訓練規模**：每個訓練條件60個情境、6個epoch、每次抽樣最多160對正/負樣本、學習率0.01——刻意輕量，方便解釋。

**exact DP的定位**：不是近似算法，是精確解，前提是兩個結構假設——(1)排序後最優解必為連續分段(2)RB是同質可互換資源(每組需要的RB數只跟選擇的品質等級有關，不跟哪些實體RB有關)。這兩個假設讓問題落在多項式可解的特例，不是文獻上證明NP-hard的一般情況(Zuhra et al. 2018的NP-hard證明用的是RB異質、任意utility oracle的一般模型，跟這裡不同)。

---

## 四、誠實的邊界(被問局限性時直接講，不用迴避)

- 學出來的方法平均只贏1%左右，不是壓倒性優勢，優勢集中在特定情境。
- resource-cost/multi-feature手刻特徵已知不穩定，這是我後續改用embedding的動機，不是遮起來的缺點。
- 真實Simu5G驗證只有15個樣本、單一次模擬(沒有多個亂數種子)，是方向性佐證，不是嚴謹的統計確認。
- LE-GRA沒有在真實資料上訓練測試過，因為15個情境遠不夠訓練一個神經網路，這是誠實的scope邊界，不是我沒想到。
- exact DP在使用者數量到數百人以上會變慢(大約n^2.3成長)，這也是為什麼還在探索windowed版本，但這部分還沒做完，不用主動提。

---

## 五、常見追問Q&A

**Q: 為什麼還要用k-means？現在都用deep learning了。**
A: k-means本身不是問題，問題是分群依據的特徵。我把k-means保留當作分群的最後一步(可解釋、確定性)，但讓一個學出來的embedding來決定分群依據的空間，而不是手動設計特徵。這樣兼顧可解釋性跟表達能力。

**Q: 你怎麼確定學出來的方法沒有過擬合/不會在真實環境失敗？**
A: 三層驗證——(1)pre-registered、非搜尋式的合成模擬器驗證，涵蓋5種情境模式x3種負載，Holm校正；(2)針對resource-cost/multi-feature的機制做過落地案例分析，不只看統計數字；(3)在真實Simu5G+SUMO資料上重新驗證，方向一致。

**Q: 為什麼一開始選的手刻特徵方法後來被推翻？這代表你的方法論有問題嗎？**
A: 不是方法論問題，是誠實回報負面結果的紀律——我先驗證了它不穩定，深入找出離群值敏感的具體機制，用實際案例展示原因，這個過程本身比一次就找到乾淨答案更能展現排查能力。

**Q: LE-GRA會不會有時候比原本的CQI分群差？**
A: 單獨用的話會，我量化過原因(訓練樣本有限、罕見邊界判斷不穩)，並加了一個保底機制(取兩者效用較高者)從結構上保證不會比CQI差，這個機制本身有數學證明支持。

**Q: 這個工作發表了嗎？**
A: 沒有，這是我發表論文之後自己延伸做的探索，用來加深對這個問題的理解，不是投稿內容。

---

## 六、絕對紅線(誠信原則，提醒自己)

- 不要說LE-GRA/resource-cost/exact DP/真實Simu5G驗證是論文裡的內容——這些都是論文之後的個人延伸。
- 如果被問到論文細節但記不清楚，就誠實說「這部分要回去查一下論文」，不要臨場編造。
- 不要用「thesis跟投稿版本不同」這類說法解釋任何不一致——誠實的「後續延伸」框架完全足夠，且沒有下檔風險。

---

## 七、一句話記憶點(電梯裡默念用)

1. 「分群本質不變，但分群依據的空間從手刻特徵變成學出來的embedding。」
2. 「效用評估本來就是k-GBRM自己的設計原則，我只是多套用一層去選方法。」
3. 「贏的地方不多，但贏在CQI真正看不到的地方，且結構上保證不會變差。」
